<%= form_for(@user_validate_only_db) do |f| %>
  <% if @user_validate_only_db.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_validate_only_db.errors.count, "error") %> prohibited this user_validate_only_db from being saved:</h2>

      <ul>
      <% @user_validate_only_db.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, size: 30 %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
